name: found-footy
prefect-version: 3.0.0

build: null
push: null
pull: null

deployments:
  # 1. Daily Ingest - ALL TEAMS (UEFA + FIFA)
  - name: fixtures-ingest-daily
    entrypoint: found_footy.flows.fixtures_flows:fixtures_ingest_flow
    work_pool:
      name: fixtures-pool
    schedule:
      cron: "5 0 * * *"
      timezone: UTC
      active: false
    parameters:
      date_str: null
      team_ids: "{{ all_teams_2025_ids }}"
    tags:
      - fixtures
      - daily
      - all-teams
    description: "Daily fixtures ingest - all tracked teams (UEFA clubs + FIFA nationals)"

  # 2. Manual ingest
  - name: fixtures-ingest-manual
    entrypoint: found_footy.flows.fixtures_flows:fixtures_ingest_flow
    work_pool:
      name: fixtures-pool
    parameters:
      date_str: "20250902"
      team_ids: "{{ all_teams_2025_ids }}"
    tags:
      - fixtures
      - manual
    description: "🎯 MANUAL: Mon Sep 02, 2025 - All Teams (UEFA + FIFA)"

  # 3. Universal Fixtures Advance Flow
  - name: fixtures-advance-flow
    entrypoint: found_footy.flows.fixtures_flows:fixtures_advance_flow
    work_pool:
      name: fixtures-pool
    parameters:
      source_collection: "fixtures_staging"
      destination_collection: "fixtures_active"
      fixture_id: 0
    tags:
      - fixtures
      - advance
    description: "Universal fixture advancement with contextual naming"

  # 4. Fixtures Monitor Flow - Always Running
  - name: fixtures-monitor-flow
    entrypoint: found_footy.flows.fixtures_flows:fixtures_monitor_flow
    work_pool:
      name: fixtures-pool
    schedule:
      cron: "*/3 * * * *"
      timezone: UTC
      active: true
    parameters: {}
    tags:
      - fixtures
      - monitor
    description: "Always-running monitor with timestamp context"

  # 5. Twitter Search Flow
  - name: twitter-search-flow
    entrypoint: found_footy.flows.twitter_flows:twitter_search_flow
    work_pool:
      name: twitter-pool
    parameters:
      goal_id: null
    tags:
      - twitter
      - automation
    description: "Event-driven Twitter processing with goal context"