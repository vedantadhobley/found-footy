// Package logging is the structured JSON emission layer: Emit(level,
// module, action, msg, fields...) with vocabulary-typed module + action.
// Every adapter + activity + workflow emits through here. See §11.
package logging
